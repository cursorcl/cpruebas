CREATE TABLE ALUMNO (ID BIGINT NOT NULL, NAME VARCHAR(255), PATERNO VARCHAR(255), RUT VARCHAR(255), MATERNO VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE ASIGNATURA (ID BIGINT NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 1)
